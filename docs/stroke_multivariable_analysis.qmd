---
title: "Facteurs associés à l'accident vasculaire cérébral chez les adultes : une analyse multivariée avec imputations multiples"
subtitle: "Analyse transversale du jeu de données de prédiction des AVC de Kaggle"
author: "Jiahui YI / Jade YI"
date: "20/09/2026"
bibliography: references.bib
lang: fr

format:
  html:
    toc: true
    toc-depth: 3
    number-sections: true
    code-fold: true
    embed-resources: true
    theme: cosmo

  pdf:
    toc: true
    toc-depth: 3
    number-sections: true

execute:
  echo: true
  warning: false
  message: false
---

# Introduction et objectifs

## Objectifs de l'analyse

L'accident vasculaire cérébral (AVC) est un problème majeur de santé publique, à la fois par sa mortalité et par les séquelles qu'il entraîne. Identifier les facteurs associés à sa survenue permet de mieux cibler la prévention.

## Méthode

L'analyse repose sur une régression logistique multivariée standard, avec l'AVC (oui/non) comme variable dépendante. Les résultats seront exprimés sous forme d'*odds ratios* (OR) ajustés, accompagnés de leur intervalle de confiance à 95 %.

Il s'agit d'une analyse observationnelle : les associations mises en évidence ne permettent pas, à elles seules, de conclure à un lien causal.# Introduction et objectifs

# Source des données

```{r}
#| label: setup
#| include: false

library(tidyverse)
library(tidyverse)
library(gtsummary)
library(broom)
library(ggdag)
```

Les données proviennent du jeu de données public *Stroke Prediction Dataset*, diffusé sur la plateforme Kaggle (fichier `healthcare-dataset-stroke-data.csv`). Chaque ligne correspond à un individu, décrit par :

- des caractéristiques démographiques (âge, sexe, statut marital, type d'emploi, lieu de résidence) ;
- des caractéristiques cliniques (hypertension, maladie cardiaque, glycémie moyenne, indice de masse corporelle) ;
- un comportement (statut tabagique) ;
- la survenue ou non d'un AVC.

```{r}
#| label: import-donnees

donnees <- read_csv(
  "healthcare-dataset-stroke-data.csv",
  na = c("", "NA", "N/A")
)

```

## Structure initiale

```{r}
#| label: structure-dimensions

n_total <- nrow(donnees)
n_avc   <- sum(donnees$stroke == 1)
n_non   <- n_total - n_avc
prop_avc <- n_avc / n_total
ic_avc  <- binom.test(n_avc, n_total)$conf.int
```

Le fichier brut contient `r n_total` individus et `r ncol(donnees)` variables. Chaque `id` est-il unique ? `r if (anyDuplicated(donnees$id) == 0) "Oui" else "Non"`.

### Répartition de l'AVC

```{r}
#| label: repartition-avc

donnees |>
  count(stroke) |>
  mutate(
    AVC = if_else(stroke == 1, "Oui", "Non"),
    Proportion = scales::percent(n / sum(n), accuracy = 0.01)
  ) |>
  select(AVC, Effectif = n, Proportion) |>
  knitr::kable()
```

```{r}
#| label: graphique-avc
#| fig-cap: "Répartition proportionnelle des individus selon la survenue d'un AVC"

donnees |>
  count(stroke) |>
  mutate(
    AVC = if_else(stroke == 1, "Oui", "Non"),
    proportion = n / sum(n)
  ) |>
  ggplot(aes(x = AVC, y = proportion)) +
  geom_col(width = 0.6) +
  geom_text(
    aes(label = scales::percent(proportion, accuracy = 0.1)),
    vjust = -0.5
  ) +
  scale_y_continuous(
    labels = scales::percent,
    limits = c(0, 1)
  ) +
  labs(x = "AVC", y = "Proportion") +
  theme_minimal()
```

L'AVC concerne `r n_avc` individus sur `r n_total`, soit `r scales::percent(prop_avc, accuracy = 0.1)` (IC à 95 % : `r scales::percent(ic_avc[1], accuracy = 0.1)` à `r scales::percent(ic_avc[2], accuracy = 0.1)`). Le rapport est d'environ `r round(n_non / n_avc, 1)` individus sans AVC pour un individu avec AVC.

**Comparaison avec la prévalence nationale.** À titre de repère, Santé publique France estime la prévalence de l'AVC dans la population adulte française à 2,0 % en 2022, soit environ 1 086 795 cas prévalents [@spf2025avc]. La proportion observée dans notre échantillon (`r scales::percent(prop_avc, accuracy = 0.1)`, IC 95 % `r scales::percent(ic_avc[1], accuracy = 0.1)`–`r scales::percent(ic_avc[2], accuracy = 0.1)`) est très supérieure à ce repère national, et l'intervalle de confiance ne le recouvre pas.

Cet écart doit toutefois être interprété avec prudence : l'indicateur de Santé publique France est une *prévalence* en population générale adulte au 1ᵉʳ janvier 2023, tandis que la variable `stroke` de notre jeu de données porte sur un échantillon dont le mode de constitution n'est pas documenté et qui inclut également des enfants. Les deux quantités ne sont donc pas strictement comparables terme à terme. Cela dit, l'ampleur de l'écart (plus du double) suggère fortement que l'échantillon n'a pas été prélevé au hasard dans la population générale, et probablement sur-représente les cas d'AVC (échantillonnage cas-témoins ou similaire). En conséquence, les résultats de cette analyse doivent être interprétés comme des associations internes à cet échantillon, et non extrapolés en population générale sans précaution.

# Évaluation initiale de la qualité des données

## Valeurs manquantes

```{r}
#| label: valeurs-manquantes

donnees |>
  summarise(across(everything(), ~ sum(is.na(.)))) |>
  pivot_longer(everything(), names_to = "Variable", values_to = "n_manquant") |>
  filter(n_manquant > 0) |>
  mutate(Pourcentage = scales::percent(n_manquant / nrow(donnees), accuracy = 0.1)) |>
  rename(`Valeurs manquantes` = n_manquant) |>
  knitr::kable()
```

Seule la variable `bmi` comporte des valeurs manquantes au sens strict (codées `NA`). Les autres variables ne présentent aucune valeur manquante déclarée.

**Cas particulier de `smoking_status`.** Cette variable comporte une modalité `"Unknown"`, qui correspond fonctionnellement à une information manquante mais qui n'est pas codée comme `NA` dans le fichier source. Cette modalité n'est pas traitée dans cette section ; elle sera examinée séparément lors de la préparation des données pour la modélisation.

À ce stade, nous nous limitons à documenter l'ampleur des valeurs manquantes de `bmi`. Leur traitement (exclusion ou imputation simple) sera décidé dans la section consacrée à la préparation des données pour la modélisation, en fonction de la stratégie retenue pour l'analyse multivariée.

## Valeurs incohérentes ou improbables

```{r}
#| label: valeurs-improbables

donnees |>
  summarise(
    `Âge min` = min(age),
    `Âge max` = max(age),
    `BMI min` = min(bmi, na.rm = TRUE),
    `BMI max` = max(bmi, na.rm = TRUE),
    `Glycémie min` = min(avg_glucose_level),
    `Glycémie max` = max(avg_glucose_level)
  ) |>
  knitr::kable(digits = 1)
```

L'examen des étendues ne révèle pas de valeur aberrante évidente (âge négatif, BMI ou glycémie nul, etc.). Deux points méritent toutefois d'être signalés :

- **Âges non entiers.** `r sum(donnees$age != round(donnees$age))` individus ont un âge non entier, tous âgés de moins de 2 ans. Il s'agit vraisemblablement d'un encodage de l'âge en fraction d'année chez les nourrissons, et non d'une erreur de saisie.
- **BMI extrême.** La valeur maximale de BMI (`r max(donnees$bmi, na.rm = TRUE)`) est cliniquement très élevée. Cette valeur sera examinée individuellement lors de la préparation des données, sans être exclue à ce stade.

```{r}
#| label: coherence-gender

donnees |> count(gender) |> knitr::kable()
```

La variable `gender` ne compte qu'une seule observation `"Other"`, effectif insuffisant pour l'analyse ; cette observation sera exclue.

Aucune autre incohérence logique n'a été identifiée (âge/statut marital, âge/type d'emploi).

**Stratégie retenue.** Le BMI maximal reste cliniquement possible : aucune modification n'est appliquée à ce stade. L'analyse sera d'abord réalisée sur les données brutes, puis en analyse de sensibilité après écrêtement (*winsorizing*) du BMI aux 2,5ᵉ et 97,5ᵉ percentiles. Les deux résultats seront comparés.

# Définition de la population analytique et préparation des données

L'analyse porte uniquement sur les adultes : les individus âgés de moins de 18 ans sont exclus, car le mécanisme et les facteurs de risque de l'AVC diffèrent fortement chez l'enfant, et plusieurs variables (`work_type`, `ever_married`) n'ont pas de sens pour cette population. L'unique observation `gender = "Other"` est également exclue, son effectif étant insuffisant pour être traitée comme catégorie à part entière.

```{r}
#| label: population-analytique

donnees_adultes <- donnees |>
  filter(age >= 18, gender != "Other") |>
  mutate(gender = droplevels(as.factor(gender)))
donnees_adultes <- donnees_adultes |>
  filter(work_type != "Never_worked") |>
  mutate(work_type = droplevels(as.factor(work_type)))

n_exclus_age    <- sum(donnees$age < 18)
n_exclus_gender <- sum(donnees$age >= 18 & donnees$gender == "Other")
n_analyse       <- nrow(donnees_adultes)
```

Sur les `r nrow(donnees)` individus du fichier initial, `r n_exclus_age` sont exclus pour âge inférieur à 18 ans et `r n_exclus_gender` pour la catégorie `gender = "Other"`, soit une population analytique de `r n_analyse` individus.

Le BMI comporte `r sum(is.na(donnees_adultes$bmi))` valeurs manquantes (`r scales::percent(sum(is.na(donnees_adultes$bmi))/n_analyse, accuracy = 0.1)`). Ce taux étant faible, les valeurs manquantes sont imputées par la médiane, sans recours à une imputation multiple.

```{r}
#| label: preparation-variables

mediane_bmi <- median(donnees_adultes$bmi, na.rm = TRUE)
seuil_bas   <- quantile(donnees_adultes$bmi, 0.025, na.rm = TRUE)
seuil_haut  <- quantile(donnees_adultes$bmi, 0.975, na.rm = TRUE)


donnees_adultes <- donnees_adultes |>
  mutate(
    bmi = if_else(is.na(bmi), mediane_bmi, bmi),
    bmi_wins = case_when(
      bmi < seuil_bas  ~ seuil_bas,
      bmi > seuil_haut ~ seuil_haut,
      TRUE ~ bmi
    ),
    stroke        = factor(stroke, levels = c(0, 1), labels = c("Non", "Oui")),
    hypertension  = factor(hypertension, levels = c(0, 1), labels = c("Non", "Oui")),
    heart_disease = factor(heart_disease, levels = c(0, 1), labels = c("Non", "Oui"))
  )
```

Après imputation, deux versions du BMI sont disponibles : `bmi` (valeurs brutes, hors valeur maximale extrême) et `bmi_wins`, où les valeurs inférieures au 2,5ᵉ percentile ou supérieures au 97,5ᵉ percentile sont ramenées à ces seuils. `r sum(donnees_adultes$bmi < seuil_bas)` valeur(s) sont ramenées au seuil inférieur (`r round(seuil_bas, 1)`) et `r sum(donnees_adultes$bmi > seuil_haut)` au seuil supérieur (`r round(seuil_haut, 1)`).

**Analyse principale et analyse de sensibilité.** L'analyse principale sera conduite avec `bmi_wins`. Une analyse de sensibilité sera réalisée avec `bmi` brut afin de vérifier que les estimations ne sont pas portées par la valeur extrême.

# Méthodes statistiques  

L'association entre chaque facteur et la survenue d'un AVC est d'abord décrite en analyse univariée (tableau 1), puis évaluée par régression logistique multivariée, l'AVC (oui/non) étant la variable dépendante. Les résultats sont exprimés en *odds ratios* (OR) ajustés avec leur intervalle de confiance à 95 %. Le seuil de significativité est fixé à 0,05.

Les variables d'ajustement ont été choisies a priori, en particulier au regard du rôle de confusion potentiel de l'âge (voir schéma ci-dessous). Deux analyses de sensibilité sont réalisées : l'une sur le BMI non écrêté (par opposition au BMI écrêté du modèle principal), l'autre en exprimant la glycémie moyenne par tranche de 10 mg/dL plutôt que par unité.

Toutes les analyses sont réalisées avec R, à l'aide des packages `tidyverse`, `gtsummary` et `broom`.
## Structure causale hypothétique (DAG)

Le graphe ci-dessous représente la structure de relations formulée a priori dans les hypothèses (section « Hypothèses et prédictions »), avant l'analyse des données. Il ne s'agit pas d'une structure confirmée, mais d'une représentation graphique du raisonnement ayant guidé le choix des variables d'ajustement, centré sur l'âge comme facteur de confusion principal.

```{r}
#| label: dag-hypotheses
#| fig-cap: "Structure causale hypothétique guidant l'analyse"
#| fig-width: 9
#| fig-height: 6

coords <- list(
  x = c(age = 2, hypertension = 3, heart_disease = 3, ever_married = 3,
        work_type = 3, gender = 2, smoking = 3, avg_glucose_level = 4,
        bmi = 4, stroke = 5),
  y = c(age = 3, hypertension = 4, heart_disease = 5, ever_married = 2,
        work_type = 1, gender = 0, smoking = 0.5, avg_glucose_level = 3.5,
        bmi = 2.5, stroke = 3)
)

dag_avc <- dagify(
  stroke ~ age + hypertension + heart_disease + avg_glucose_level + bmi + smoking + gender,
  hypertension ~ age,
  heart_disease ~ age,
  ever_married ~ age,
  work_type ~ age,
  smoking ~ gender,
  exposure = "age",
  outcome = "stroke",
  coords = coords,
  labels = c(
    stroke             = "AVC",
    age                = "Âge",
    gender             = "Sexe",
    hypertension       = "Hypertension",
    heart_disease      = "Maladie\ncardiaque",
    avg_glucose_level  = "Glycémie",
    bmi                = "IMC",
    smoking            = "Tabac",
    ever_married       = "Statut\nmarital",
    work_type          = "Type\nd'emploi"
  )
)

ggplot(dag_avc, aes(x = x, y = y, xend = xend, yend = yend)) +
  geom_dag_edges() +
  geom_label(aes(label = label), na.rm = TRUE, size = 3.2) +
  theme_dag()
```

# Résultats  

## Description de la population

Le tableau 1 décrit les caractéristiques de la population analytique selon la survenue ou non d'un AVC.

```{r}
#| label: table1

donnees_adultes |>
  select(
    stroke, age, gender, hypertension, heart_disease,
    ever_married, work_type, Residence_type,
    avg_glucose_level, bmi_wins, smoking_status
  ) |>
  tbl_summary(
    by = stroke,
    label = list(
      age                ~ "Âge (années)",
      gender             ~ "Sexe",
      hypertension       ~ "Hypertension",
      heart_disease      ~ "Maladie cardiaque",
      ever_married       ~ "Déjà marié(e)",
      work_type          ~ "Type d'emploi",
      Residence_type     ~ "Lieu de résidence",
      avg_glucose_level  ~ "Glycémie moyenne (mg/dL)",
      bmi_wins           ~ "IMC (kg/m², écrêté)",
      smoking_status     ~ "Statut tabagique"
    ),
    statistic = list(
      all_continuous()  ~ "{mean} ({sd})",
      all_categorical() ~ "{n} ({p}%)"
    ),
    digits = all_continuous() ~ 1
  ) |>
  add_p(test = list(all_continuous() ~ "t.test")) |>
  add_overall() |>
  modify_header(label ~ "**Variable**") |>
  modify_caption("**Tableau 1.** Caractéristiques de la population selon le statut AVC")
```

Les variables continues sont présentées en moyenne (écart-type) et comparées par test t de Student ; les variables catégorielles sont présentées en effectif (pourcentage) et comparées par test du χ² (ou test exact de Fisher en cas d'effectifs attendus trop faibles).

## Modèle multivarié

Un modèle de régression logistique multivariée est ajusté, avec la survenue d'un AVC comme variable dépendante. Les covariables incluses sont l'âge, le sexe, l'hypertension, la maladie cardiaque, le statut marital, le type d'emploi, le lieu de résidence, la glycémie moyenne, l'IMC (version écrêtée) et le statut tabagique — soit l'ensemble des variables décrites au tableau 1.

```{r}
#| label: modele-principal

modele_principal <- glm(
  stroke ~ age + gender + hypertension + heart_disease + ever_married +
    work_type + Residence_type + avg_glucose_level + bmi_wins + smoking_status,
  data = donnees_adultes,
  family = binomial(link = "logit")
)

tbl_regression(modele_principal, exponentiate = TRUE) |>
  modify_header(label ~ "**Variable**") |>
  modify_caption("**Tableau 2.** Odds ratios ajustés (IC 95%) de l'association avec l'AVC")
```

Après ajustement, l'âge (`OR = 1.08` par année, IC 95 % 1.07–1.09, p < 0,001), l'hypertension (`OR = 1.49`, IC 95 % 1.07–2.05, p = 0,015) et la glycémie moyenne (`OR = 1.00` par mg/dL, IC 95 % 1.00–1.01, p < 0,001) sont significativement et indépendamment associées à l'AVC.

Les autres covariables ne sont pas significativement associées à l'AVC dans ce modèle ajusté : le sexe, la maladie cardiaque, le statut marital, le type d'emploi, le lieu de résidence, l'IMC (version écrêtée) et le statut tabagique.

La maladie cardiaque, significativement associée à l'AVC en analyse univariée (tableau 1), ne l'est plus après ajustement (`OR = 1.32`, IC 95 % 0.90–1.90, p = 0.15). Cette atténuation suggère que l'association observée en univariée est en partie expliquée par l'âge, les patients porteurs d'une maladie cardiaque étant en moyenne plus âgés.

**Cas particulier de la glycémie moyenne.** Bien que statistiquement significatif, l'OR de `avg_glucose_level` est très proche de 1 (1,00 par unité de mg/dL). Cela reflète l'échelle de la variable : un écart de 1 mg/dL est cliniquement négligeable, alors qu'un écart de 20 ou 30 mg/dL est cliniquement pertinent. L'ampleur réelle de l'effet est donc sous-estimée visuellement par cet OR unitaire.

```{r}
#| label: modele-glucose-echelle

donnees_adultes <- donnees_adultes |>
  mutate(glucose_10 = avg_glucose_level / 10)

modele_glucose <- glm(
  stroke ~ age + gender + hypertension + heart_disease + ever_married +
    work_type + Residence_type + glucose_10 + bmi_wins + smoking_status,
  data = donnees_adultes,
  family = binomial(link = "logit")
)

or_glucose <- exp(coef(modele_glucose)["glucose_10"])
ic_glucose <- exp(confint.default(modele_glucose)["glucose_10", ])
```

Exprimé pour une augmentation de 10 mg/dL, l'effet de la glycémie moyenne devient plus interprétable : OR = `r round(or_glucose, 2)` (IC 95 % `r round(ic_glucose[1], 2)`–`r round(ic_glucose[2], 2)`). Les coefficients des autres covariables sont strictement identiques à ceux du modèle principal, seule l'échelle de `avg_glucose_level` étant modifiée.

## Analyse de sensibilité (BMI)

Afin de vérifier que les résultats ne sont pas portés par la valeur extrême de BMI identifiée en évaluation de la qualité des données, le modèle principal est reproduit à l'identique en remplaçant `bmi_wins` par le BMI brut (non écrêté).

```{r}
#| label: modele-sensibilite-bmi

modele_bmi_brut <- glm(
  stroke ~ age + gender + hypertension + heart_disease + ever_married +
    work_type + Residence_type + avg_glucose_level + bmi + smoking_status,
  data = donnees_adultes,
  family = binomial(link = "logit")
)

tbl_regression(modele_bmi_brut, exponentiate = TRUE) |>
  modify_header(label ~ "**Variable**") |>
  modify_caption("**Tableau 3.** Modèle avec BMI brut (analyse de sensibilité)")
```

```{r}
#| label: comparaison-bmi

or_wins <- exp(coef(modele_principal)["bmi_wins"])
or_brut <- exp(coef(modele_bmi_brut)["bmi"])
```

Le coefficient du BMI reste non significatif dans les deux versions du modèle (BMI écrêté : OR = `r round(or_wins, 2)` ; BMI brut : OR = `r round(or_brut, 2)`), et les coefficients des autres covariables ne sont pas modifiés de façon substantielle. Les résultats de l'analyse principale ne sont donc pas sensibles à la valeur extrême de BMI identifiée précédemment.

## Représentation graphique des résultats

```{r}
#| label: foret-modele-principal
#| fig-cap: "Odds ratios ajustés (IC 95%) du modèle principal"
#| fig-width: 7
#| fig-height: 6

etiquettes <- c(
  age                        = "Âge",
  genderMale                 = "Sexe : Homme",
  hypertensionOui            = "Hypertension",
  heart_diseaseOui           = "Maladie cardiaque",
  ever_marriedYes            = "Déjà marié(e)",
  work_typePrivate           = "Emploi : Privé",
  `work_typeSelf-employed`   = "Emploi : Indépendant",
  Residence_typeUrban        = "Résidence : Urbaine",
  avg_glucose_level          = "Glycémie moyenne",
  bmi_wins                   = "IMC (écrêté)",
  `smoking_statusnever smoked` = "Tabac : jamais fumé",
  smoking_statussmokes       = "Tabac : fumeur actuel",
  smoking_statusUnknown      = "Tabac : inconnu"
)

resultats_modele <- tidy(modele_principal, exponentiate = TRUE, conf.int = TRUE) |>
  filter(term != "(Intercept)") |>
  mutate(term = recode(term, !!!etiquettes))

ggplot(resultats_modele, aes(x = estimate, y = fct_reorder(term, estimate))) +
  geom_vline(xintercept = 1, linetype = "dashed", color = "grey50") +
  geom_pointrange(aes(xmin = conf.low, xmax = conf.high)) +
  scale_x_log10() +
  labs(
    x = "Odds ratio (échelle log)",
    y = NULL,
    title = "Facteurs associés à l'AVC (modèle ajusté)"
  ) +
  theme_minimal()
```


Ce graphe formalise le raisonnement qui a guidé la sélection des variables d'ajustement : l'âge est traité comme un facteur de confusion central, susceptible d'expliquer une partie des associations brutes observées pour l'hypertension, la maladie cardiaque, le statut marital et le type d'emploi. Les résultats du modèle ajusté (tableau 2) sont cohérents avec cette structure : les associations portées uniquement par l'âge (statut marital, type d'emploi) disparaissent après ajustement, tandis que celles ayant une composante propre (hypertension, glycémie) persistent, au moins partiellement.


# Discussion

## Confrontation aux hypothèses

**Âge (H1).** L'association prédite se confirme et reste la plus forte du modèle (OR = 1,08 par année, p < 0,001). Elle persiste après ajustement, comme prévu.

**Hypertension (H2).** L'association reste significative après ajustement (OR = 1,49, p = 0,015), mais son intensité diminue par rapport à l'analyse brute. L'hypothèse d'une atténuation partielle par l'âge se vérifie, sans disparition complète de l'effet.

**Glycémie moyenne (H3).** L'association prédite se confirme. Exprimé par unité de mg/dL, l'OR est très proche de 1, ce qui reflète l'échelle de la variable plutôt qu'une absence d'effet réel : un écart de 1 mg/dL est cliniquement négligeable, alors qu'un écart plus important (par exemple 10 mg/dL) l'est davantage. Exprimé sur cette échelle, l'effet reste significatif mais modeste, sans permettre de conclure sur les mécanismes sous-jacents (âge, IMC, hypertension).

**IMC (H4).** Contrairement à la prédiction, aucune association significative n'est observée, ni avec le BMI brut ni avec le BMI écrêté — les deux versions donnent des résultats quasi identiques (analyse de sensibilité). L'hypothèse d'un effet propre de l'IMC sur l'AVC, indépendamment de l'hypertension et de la glycémie, n'est pas soutenue par ces données. Une relation non linéaire, évoquée comme possibilité en H4, n'a pas été testée ici et pourrait expliquer en partie cette absence d'effet.

**Tabagisme (H5).** Aucune des catégories n'est significativement associée à l'AVC après ajustement. L'hypothèse d'un effet propre du tabagisme n'est pas confirmée dans ce modèle.

**Type d'emploi (H6).** Conformément à la prédiction, aucune association forte ne persiste après ajustement. Cette variable se confirme comme peu informative pour expliquer l'AVC une fois l'âge pris en compte.

**Type de résidence (H7).** Aucune association observée, ni en brut ni en ajusté, conformément à la prédiction.

**Statut matrimonial (H8).** L'hypothèse se confirme : l'association observée en analyse univariée disparaît après ajustement sur l'âge (OR = 0,82, p = 0,4). Cela illustre bien le phénomène anticipé d'une association trompeuse portée par l'âge.

**Maladie cardiaque (H9).** L'association prédite comme forte s'atténue nettement après ajustement et devient non significative (OR = 1,32, p = 0,15). L'hypothèse prévoyait une atténuation partielle mais un effet résiduel important ; ce n'est pas ce qui est observé ici, l'âge semblant expliquer une grande partie de l'association brute — les patients porteurs d'une maladie cardiaque étant en moyenne plus âgés.

**Genre (H10).** Aucune différence significative entre hommes et femmes n'est observée dans le modèle ajusté (OR = 1,04, p = 0,8). L'hypothèse d'un effet médié par le tabagisme n'a donc pas lieu d'être testée, faute d'association de départ à expliquer.

## Limites

La proportion d'AVC dans cet échantillon (4,9 %) est très supérieure à la prévalence nationale française (2,0 %), ce qui suggère un mode d'échantillonnage non aléatoire. Les résultats doivent donc être interprétés comme des associations internes à cet échantillon, sans extrapolation à la population générale.

Le traitement des valeurs manquantes du BMI par imputation simple (médiane) constitue une autre limite : cette méthode réduit artificiellement la variance de la variable, ce qui peut conduire à sous-estimer son rôle réel.

Enfin, le caractère transversal des données ne permet pas d'établir de séquence temporelle entre les facteurs étudiés et la survenue de l'AVC, ce qui limite toute interprétation causale.

# Conclusion

Parmi les facteurs étudiés, l'âge, l'hypertension et la glycémie moyenne restent indépendamment associés à l'AVC après ajustement. Les associations observées pour la maladie cardiaque et le statut matrimonial dans l'analyse brute s'expliquent en grande partie par l'âge. L'IMC, le tabagisme, le type d'emploi, le type de résidence et le genre ne montrent pas d'association indépendante dans cet échantillon.